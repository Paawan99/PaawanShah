# Legacy portfolio page

The original `index.html` from `main` is preserved at the repo root for now;
when the SAAS launches we'll either:

1. Move it to `legacy/index.html` and serve it under `/legacy` from Vercel, or
2. Move the SAAS to a subdomain (e.g. `astro.paawanshah99.com`) and leave the
   apex pointing at the existing portfolio.

Decision deferred until the domain DNS strategy is finalized.
